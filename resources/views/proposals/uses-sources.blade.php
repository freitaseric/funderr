@foreach(['uses' => 'Usos', 'sources' => 'Fontes'] as $key => $title)
<div class="overflow-x-auto"><table class="table table-sm"><caption class="text-left font-semibold">{{ $title }}</caption><thead><tr><th>Discriminação</th><th>Realizado (R$)</th><th>A realizar (R$)</th><th>Total (R$)</th></tr></thead><tbody>@foreach($usesSources[$key] as $row)<tr><th>{{ $row['label'] }}</th><td class="whitespace-nowrap bg-base-200/50">{{ number_format($row['realized'], 2, ',', '.') }}</td><td class="whitespace-nowrap">{{ number_format($row['planned'], 2, ',', '.') }}</td><td class="whitespace-nowrap font-semibold">{{ number_format($row['total'], 2, ',', '.') }}</td></tr>@endforeach</tbody><tfoot><tr><th colspan="3">Total</th><td>{{ number_format($usesSources[$key.'_total'], 2, ',', '.') }}</td></tr></tfoot></table></div>
@endforeach
@if(abs($usesSources['difference']) >= 0.01)<p class="rounded-lg bg-warning/15 p-3 text-sm">Diferença entre fontes e usos: R$ {{ number_format($usesSources['difference'], 2, ',', '.') }}. Confira os investimentos a realizar e as fontes adicionais.</p>@endif

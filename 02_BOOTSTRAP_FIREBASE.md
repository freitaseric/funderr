# Prompt 02 — scaffold + Firebase

Com base na auditoria aprovada:

1. crie React 19 + TypeScript;
2. use GovBR-DS React oficial;
3. configure router/layout;
4. configure testes, lint e typecheck;
5. habilite integração Firebase Firestore + Authentication;
6. antes de confirmar o provisionamento, mostre:
   - Google Cloud/Firebase project ID;
   - localização;
   - database ID;
7. usar `southamerica-east1`, salvo decisão explícita diferente;
8. configure login Google;
9. crie `users/{uid}` e fluxo de usuário pendente de autorização;
10. gere regras deny-by-default;
11. crie emuladores/configuração local se suportado;
12. não migre entidades ainda.

Execute typecheck/test e reporte.
